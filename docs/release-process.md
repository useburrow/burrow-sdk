# Release Process

1. Update `spec/contracts` fixtures if contract behavior changed.
2. Run tests in `php/`.
3. Bump package version per SemVer.
4. Update changelog with migration notes for breaking changes.
5. Tag release and publish package.
