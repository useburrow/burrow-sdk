# Versioning Policy

Burrow SDK packages follow SemVer.

- `MAJOR`: breaking API or contract behavior changes
- `MINOR`: backwards-compatible features
- `PATCH`: bug fixes/internal improvements

## Compatibility

- SDK major versions map to Burrow contract generations.
- Burrow should support at least current and previous SDK major during migration windows.
